# URL backend API (без слэша в конце).
# Локально можно оставить пустым — Vite проксирует /api на localhost:8000.
# VITE_API_URL=http://127.0.0.1:8000
#
# Production (Render), пример:
# VITE_API_URL=https://pptx-checker-api.onrender.com
